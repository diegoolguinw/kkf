# Disabled legacy workflow.
# Publishing is handled by .github/workflows/python-publish.yml
name: Build & Publish (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow has been disabled. Use python-publish.yml instead."
